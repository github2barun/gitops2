---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: logs-template-management-{{ .Release.Namespace }}
  labels:
    type: local
    name: logs-template-management-{{ .Release.Namespace }}
spec:
  storageClassName: manual
  capacity:
    storage: 200Mi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "{{ .Values.logs }}/template-management-system"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-template-management-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
  selector:
    matchLabels:
      type: local
      name: logs-template-management-{{ .Release.Namespace }}
  storageClassName: manual
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: template-management
  namespace: {{ .Release.Namespace }}
  labels:
    app: template-management
spec:
  replicas: {{ .Values.replica.templatemanagement }}
  selector:
    matchLabels:
      app: template-management
  template:
    metadata:
      labels:
        app: template-management
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - template-management
            topologyKey: "kubernetes.io/hostname"
      initContainers:
      - name: copy-configs
        image: centos
        env:
        - name: NAMESPACE
          value: "{{ .Release.Namespace }}"
        - name: NAMESPACE_LOGS
          value: "{{ .Release.Namespace_logs_enabled }}"
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-template-management-main
          mountPath: /template-management-main
        - name: vol-cm-template-management-copy
          mountPath: /scripts
      containers:
      - name: template-management
        image: {{ .Values.image.templatemanagement }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8378
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        {{- if eq .Values.resourceApply true }}
        resources:
          limits:
            cpu: '1'
            memory: 3Gi
          requests:
            cpu: '0.5'
            memory: 2Gi
        {{- end }}
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /etc/localtime
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - mountPath: {{ .Values.logs }}/template-management-system
          name: vol-logs-template-management
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-template-management-main
        configMap:
          name: cm-template-management-main
      - name: vol-logs-template-management
        persistentVolumeClaim:
          claimName: logs-template-management-{{ .Release.Namespace }}
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-template-management-copy
        configMap:
          name: cm-template-management-copy
      imagePullSecrets:
      - name: {{ .Values.secret }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: template-management
  name: svc-template-management
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 8378
    targetPort: 8378
  selector:
    app: template-management
  type: ClusterIP
