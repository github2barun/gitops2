---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: logs-businesslogic-{{ .Release.Namespace }}
  labels:
    type: local
    name: logs-businesslogic-{{ .Release.Namespace }}
spec:
  storageClassName: manual
  capacity:
    storage: 200Mi
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "{{ .Values.logs }}/businesslogic"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: logs-businesslogic-{{ .Release.Namespace }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
  selector:
    matchLabels:
      type: local
      name: logs-businesslogic-{{ .Release.Namespace }}
  storageClassName: manual
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: businesslogic
  namespace: {{ .Release.Namespace }}
  labels:
    app: businesslogic
spec:
  replicas: {{ .Values.replica.businesslogic }}
  selector:
    matchLabels:
      app: businesslogic
  template:
    metadata:
      labels:
        app: businesslogic
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - businesslogic
            topologyKey: "kubernetes.io/hostname"
      initContainers:
      - name: copy-configs
        image: centos
        env:
        - name: NAMESPACE
          value: "{{ .Release.Namespace }}"
        - name: NAMESPACE_LOGS
          value: "{{ .Release.Namespace_logs_enabled }}"
        imagePullPolicy: IfNotPresent
        command: ['bash','/scripts/configs-copy']
        volumeMounts:
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-cm-main
          mountPath: /main
        - name: vol-cm-businesslogic-main
          mountPath: /businesslogic
        - name: vol-cm-businesslogic-copy
          mountPath: /scripts
      containers:
      - name: businesslogic
        image: {{ .Values.image.businesslogic }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8922
        env:
        - name: DEFAULT_TIME_ZONE
          value: "{{ .Values.timezone }}"
        - name: TZ
          value: "{{ .Values.timezone }}"
        {{- if eq .Values.resourceApply true }}
        resources:
          limits:
            cpu: '1'
            memory: 3Gi
          requests:
            cpu: '0.5'
            memory: 2Gi
        {{- end }}
        volumeMounts:
        - name: tz-{{ .Values.country }}
          mountPath: /etc/localtime
        - name: config-dir-main
          mountPath: {{ .Values.conf }}
        - name: vol-logs-businesslogic
          mountPath: {{ .Values.logs }}/businesslogic
      volumes:
      - name: tz-{{ .Values.country }}
        hostPath:
          path: /usr/share/zoneinfo/{{ .Values.timezone }}
      - name: vol-cm-main
        configMap:
          name: cm-main
      - name: vol-cm-businesslogic-main
        configMap:
          name: cm-businesslogic-main
      - name: config-dir-main
        emptyDir: {}
      - name: vol-cm-businesslogic-copy
        configMap:
          name: cm-businesslogic-copy
      - name: vol-logs-businesslogic
        persistentVolumeClaim:
          claimName: logs-businesslogic-{{ .Release.Namespace }}
      imagePullSecrets:
      - name: {{ .Values.secret }}
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: businesslogic
  name: svc-businesslogic
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 8922
    targetPort: 8922
  selector:
    app: businesslogic
  type: ClusterIP
